# Rascunhos/protótipos antigos, não usados pelo site real (index/gibi/game/musica.html)
# e muito grandes pra valer a pena versionar (~24 MB juntos).
DIGEP Por Dentro - Edição 01 - Arquivo Bruto em HTML.html
DIGEP por Dentro - Portal (visual).html

# Config local do Claude Code, não faz parte do site
.claude/

# Cache local do Wrangler (Cloudflare Pages)
.wrangler/
